/*-------------------------------------------------------
【程序设计】
---------------------------------------------------------

题目：用循环实现输入一个五位正整数，将它倒序后返回给主函数输出。
例如输入12345，输出应为54321。（不采用指针、数组）

-------------------------------------------------------*/
#include <stdio.h>

int fun(int  n)
{ 
/**********Program**********/
    int m=0;
    m=n%10*10000+n/10%10*1000+n/100%10*100+n/1000%10*10+n/10000;
return m;


/**********  End  **********/
}
int main()
{ 
   int n,r;
   scanf("%d",&n);
   r=fun(n);
   printf("%d\n", r);
   return 0;
}