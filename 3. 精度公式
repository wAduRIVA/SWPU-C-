/*-------------------------------------------------------
【程序设计】
---------------------------------------------------------

题目：输入x,计算如样图所示的多项式求和，直到|xn|<0.000001时停止。
      当输入为1时，结果为1.414214，sum用于存储多项的和，xn表示每一个项的数据。

-------------------------------------------------------*/
#include<stdio.h>
#include<math.h>
int main()
{
        int i;
        double xn=1.0 ,sum=0.0, x;
        scanf("%lf",&x);  
/**********Program**********/
        i=1;
        while(fabs(xn)>=0.000001)
        {
            sum=sum+xn;
            xn=xn*(0.5-(float)i+1)*x/i;
            i++;

        }
/**********  End  **********/                                     
        printf("%lf\n",sum);
        return 0;
}